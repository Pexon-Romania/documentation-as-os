# {{PROJECT_NAME}} — STATUS (live current-state)

> **Read at the start of every session** (the `SessionStart` hook surfaces this). **Update before you stop.** This is where the project *is* — the OS's process table.
>
> Last verified: {{DATE}}

## Current phase
{{e.g. "Phase 1 — core build · in dev" — one line}}

## Where we are
- {{1–4 bullets: what's done / what's live}}

## What's next
1. {{the next concrete action}}
<!-- A forward-looking claim that must later be confirmed (e.g. "restart to activate the hooks")? Log it in doc-debt.md as "- [ ] confirm …" the moment you write it — so it's reconciled, not just remembered (constitution §7). -->

## Blocked / waiting
- {{blockers + who owns them, or "none"}}

## Open doc-debt
> **Pointer only — `doc-debt.md` is the canonical register** (the hooks read it, not this section). Mirror the count here at write-out; empty is the goal.
- see `docs/delivery/doc-debt.md` — {{N open / none}}

## Index — the key docs
- Architecture → `docs/engineering/architecture.md`
- Decisions (why) → `docs/delivery/decisions/`
- What shipped → `docs/delivery/CHANGELOG.md`
- {{add the few that matter}}

<!-- One concern: where we are · next · blocked · debt + an index. Detail lives in the linked docs, NOT here (board = index, docs = records). Keep it scannable. -->
