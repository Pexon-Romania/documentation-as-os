# {{APP}} documentation

> **Canonical home:** `github.com/{{ORG}}/{{REPO}}` — the source of truth itself; every local copy
> is a clone or a read-only mirror. Machine-enforced: the validator gates every change.

**Route by who you are — every line is ≤3 hops:**

| You are… | Start here |
|---|---|
| **A leader** ("what is this, is it healthy?") | `leaders/` |
| **Support / a business user** | `support/` (FAQ · how-to) |
| **An engineer** ("how does it work?") | `app-documentation/` (00 → the map) |
| **Working an integration/dependency** | `dependencies/` (one doc per external system) |
| **Delivering ("what's shipping, what happened?")** | `delivery/` (board · changelog · learnings · incidents) |

**The system's own rules:** `AGENTS.md` (protocol) · `DOCS-STANDARD.md` (the contract + locked
tree) · `DOCS-SYSTEM.md` (this system explaining itself) · `MANIFEST.json` (machine navigation —
regenerated, never hand-edited).
